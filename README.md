# CustomerDatabaseTkinterGUI
Customer database desktop app built with Python and Tkinter. Users can add, search, or delete customers from the database. Uses the pandas module to store data in the customer_data.csv file located in the data folder. Program runs data validation to make sure data is properly entered before it's written to the customer_data.csv file.
